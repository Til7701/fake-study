package de.holube.fakestudy.old;

public class MyMath {
    public static double addVarianz(double value, double varianz) {
        return value = value + ((Math.random() * varianz) - (varianz / 2));
    }
}
